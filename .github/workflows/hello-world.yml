name: Hello World
on:
    push:
        branches: [ main ]
    workflow_dispatch:
jobs:
    say_hello:
        name: Print Hello World
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: echo "Hello world!"